int main()
{
    int a = 2; //зміна a
    int b, c, d, e, f; // змінні
        // a4 start
        b = (a * a); // "a" в другому степіню
        c = (b * b); // "a" в четвертому степіню
        // a4 end
        d = (b * b * b); // "a" в шостому степіню
        e = (d * b); // "a" у восьмому степіню
        f = (e * b); // "a" в десятому степіню
}